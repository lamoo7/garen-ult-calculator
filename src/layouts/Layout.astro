<!doctype html>
<html lang="en">
	<head>
		<meta charset="UTF-8" />
		<meta name="viewport" content="width=device-width, initial-scale=1.0" />
		<link rel="icon" type="image/webp" href={`${import.meta.env.BASE_URL}/favicon.ico`} />
		<meta name="generator" content={Astro.generator} />
		<title>Garen Ultimate Calculator</title>
		<meta name="description" content="Calculate Garen's ultimate damage with items, runes, and enemy stats" />
	</head>
	<body>
		<slot />
	</body>
</html>

<style>
	@import '../styles/variables.css';

	body {
		margin: 0;
		min-block-size: 100svh;
		font-family: var(--font-family);
		font-size: var(--font-size-base);
		color: var(--color-text);
		position: relative;

		display: grid;
		place-items: center;
	}
</style>